{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "@modelcontextprotocol/server-filesystem",
        "/Users/dawiddutoit/projects",
        "{{PROJECT_DIR}}"
      ],
      "type": "stdio",
      "scope": "user",
      "description": "File system access for current project"
    },
    "git": {
      "command": "uvx",
      "args": [
        "mcp-server-git",
        "{{PROJECT_DIR}}"
      ],
      "type": "stdio",
      "scope": "user",
      "description": "Git repository operations for current project"
    }
  },
  "metadata": {
    "version": "1.0.0",
    "type": "project-local",
    "description": "Project-specific MCP server overrides and additions",
    "usage": "Copy to .mcp-config.json in project root and customize as needed"
  }
}